#include <stdio.h>

int main(){

    int from_currency, to_currency;
    float converted_amount;

    printf("Enter from currency (1 for USD, 2 for EUR, 3 for INR): ");
    scanf("%d", &from_currency);

    printf("Enter to currency (1 for USD, 2 for EUR, 3 for INR): ");
    scanf("%d", &to_currency);

    if ()

}