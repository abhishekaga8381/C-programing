#include <stdio.h>

int main(){

    /*int a = 50, b = 2;
    Arthmatic operators
    int add, sub, multiplication, division, modulus;
    add = a+b;
    sub = a-b;
    multiplication = a*b;
    division = a/b;
    modulus = a%b;

    printf("addition of a and b %d\n", add);
    printf("subtraction of a and b %d\n", sub);
    printf("multiplication of a and b %d\n", multiplication);
    printf("division of a and b %d\n", division); // printf("division of a and b %f\n", a/(float)b); if we know the ans is in decimal format then we can typecast one of the operand to float    
    printf("modulus of a and b %d\n", modulus); */




    /*Relational operators
    int a=10, b=20;

    printf("\na is greater than b %d", a>b);
    printf("\na is less than b %d", a<b);   
    printf("\na is greater than or equal to b %d", a>=b);
    printf("\na is less than or equal to b %d", a<=b);
    printf("\na is equal to b %d", a==b);
    // printf("\na is not equal to b %d", a!=b);   */





    /*logical operators
    int x = 1, y = 0;

    printf("\nx and y %d", x && y);
    printf("\nx or y %d", x || y);
    printf("\nnot x %d", !x);   */


    

    /*cnditional operator
    int x = 10, y=10, z;
    printf("\nz = (x>y) ? x : y = %d", z = (x>y) ? x : y);    */





    //increment and decrement operators
    /*int x = 3, y = 6, z = 4, add; 

    add = (--x) + (++y) + (z--);

    printf("\nadd = %d", add);
    printf("\n--x = %d", x);   
    printf("\n++y = %d", y); 
    printf("\nz-- = %d", z); */

    /*int x = 1, y = 0, z;
    z = x++ || ++y;
    printf("\nz = %d", z);
    printf("\nx = %d", x);
    printf("\ny = %d", y);  */

    /*int x = 1, y = 0, z;
    z = x++ && ++y;
    printf("\nz = %d", z);
    printf("\nx = %d", x);
    printf("\ny = %d", y); */


    

    //bitwise operators
   /*int a = 3, b = 4;
    printf("\na & b = %d", a & b);
    printf("\na | b = %d", a | b);
    printf("\na ^ b = %d", a ^ b);
    printf("\na << 1 = %d", a << 1);
    printf("\nb >> 1 = %d", b >> 1);            */

    int a = 5, b = 6;
    printf("\n a << 2 = %d", a);
    printf("\n b >> 2 = %d", b);

    printf("\n a^b = %d", a^b);
    printf("\n a&b = %d", a&b);
    printf("\n a/b = %d", a/b);
}